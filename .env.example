# Nest API base (must include /api). Example: http://127.0.0.1:3000/api
VITE_API_BASE_URL=http://127.0.0.1:3000/api

# Main (tenant) SPA when the operator app is served on another port or host.
# Example: http://localhost:5173 — leave empty for same-origin + reverse proxy.
VITE_MAIN_APP_ORIGIN=
